/**
 * prompts/contradiction.prompt.ts
 *
 * Prompts for the Contradiction Detection Engine.
 *
 * TODO (Member 3 — AI): Implement these prompts to:
 *   - Accept the current answer and the history of candidateClaims
 *   - Identify any logical inconsistencies between the new answer and prior claims
 *   - Return a structured ContradictionResult
 *   - Be precise — only flag genuine logical contradictions, not nuanced differences
 *
 * Owner: Member 3 (AI / Prompt Engineering)
 */

/** System prompt establishing the Contradiction Detector persona */
export const CONTRADICTION_SYSTEM_PROMPT = "";

/**
 * User prompt template for contradiction detection.
 * Expects placeholders: {currentAnswer}, {priorClaims}
 */
export const CONTRADICTION_USER_PROMPT = "";
